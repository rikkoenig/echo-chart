apiVersion: v1
kind: Service
metadata:
  name: {{ include "echoIP.fullname" . }}
  labels:
    {{- include "echoIP.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "echoIP.selectorLabels" . | nindent 4 }}
