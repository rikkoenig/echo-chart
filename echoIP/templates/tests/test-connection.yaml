apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "echoIP.fullname" . }}-test-connection"
  labels:
    {{- include "echoIP.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "echoIP.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
